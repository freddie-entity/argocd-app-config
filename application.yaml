apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fred-argocd-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/freddie-entity/argocd-app-config.git #3poft20Lvj
    targetRevision: HEAD
    path: nodeapp
  destination: 
    server: https://kubernetes.default.svc
    namespace: myapp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true